#from django.contrib.admin.apps import AdminConfig
#from django.contrib.admin import AdminSite
#from django.apps import AppConfig



# Choose one AppConfig class to keep as the default and comment out the other
# For example, if you want to keep AgenceAdminConfig as the default
# class AdminConfig(AdminConfig):
#     default_site = 'agence.apps.AgenceAdminArea'

#class AgenceAdminArea(AdminSite):
  #  site_header = 'Agence Administration'

#class AgenceConfig(AppConfig):
   # name = 'agence'